diff --git a/pump_end_v2/config.py b/pump_end_v2/config.py
index c27a294..25b56fa 100644
--- a/pump_end_v2/config.py
+++ b/pump_end_v2/config.py
@@ -84,6 +84,8 @@ class DetectorPolicyConfig:
     fire_score_floor: float
     turn_down_delta: float
     min_peak_gain_after_arm: float
+    max_sl_row_at_fire: float
+    min_tp_sl_margin_at_fire: float
 
 
 @dataclass(frozen=True, slots=True)
@@ -92,6 +94,8 @@ class DetectorPolicySearchConfig:
     fire_candidates: tuple[float, ...]
     turn_candidates: tuple[float, ...]
     peak_gain_candidates: tuple[float, ...]
+    max_sl_row_at_fire_candidates: tuple[float, ...]
+    min_tp_sl_margin_at_fire_candidates: tuple[float, ...]
     selector_val_fires_per_30d_min: float
     selector_val_fires_per_30d_max: float
     selector_min_resolved_signals: int
@@ -725,6 +729,8 @@ def _build_detector_policy(section: dict[str, Any]) -> DetectorPolicyConfig:
     fire_score_floor = float(section.get("fire_score_floor"))
     turn_down_delta = float(section.get("turn_down_delta"))
     min_peak_gain_after_arm = float(section.get("min_peak_gain_after_arm", 0.0))
+    max_sl_row_at_fire = float(section.get("max_sl_row_at_fire", 1.0))
+    min_tp_sl_margin_at_fire = float(section.get("min_tp_sl_margin_at_fire", 0.0))
     if not (0.0 < arm_score_min <= 1.0):
         raise ValueError("detector.arm_score_min must satisfy 0 < x <= 1")
     if not (0.0 <= fire_score_floor <= arm_score_min):
@@ -737,11 +743,19 @@ def _build_detector_policy(section: dict[str, Any]) -> DetectorPolicyConfig:
         raise ValueError(
             "detector.min_peak_gain_after_arm must satisfy 0 <= x <= 1"
         )
+    if not (0.0 <= max_sl_row_at_fire <= 1.0):
+        raise ValueError("detector.max_sl_row_at_fire must satisfy 0 <= x <= 1")
+    if not (-1.0 <= min_tp_sl_margin_at_fire <= 1.0):
+        raise ValueError(
+            "detector.min_tp_sl_margin_at_fire must satisfy -1 <= x <= 1"
+        )
     return DetectorPolicyConfig(
         arm_score_min=arm_score_min,
         fire_score_floor=fire_score_floor,
         turn_down_delta=turn_down_delta,
         min_peak_gain_after_arm=min_peak_gain_after_arm,
+        max_sl_row_at_fire=max_sl_row_at_fire,
+        min_tp_sl_margin_at_fire=min_tp_sl_margin_at_fire,
     )
 
 
@@ -789,6 +803,14 @@ def _build_detector_policy_search(
         detector_policy_section.get("peak_gain_candidates", []),
         "search.detector_policy.peak_gain_candidates",
     )
+    max_sl_row_at_fire_candidates = _parse_float_candidates(
+        detector_policy_section.get("max_sl_row_at_fire_candidates", []),
+        "search.detector_policy.max_sl_row_at_fire_candidates",
+    )
+    min_tp_sl_margin_at_fire_candidates = _parse_float_candidates(
+        detector_policy_section.get("min_tp_sl_margin_at_fire_candidates", []),
+        "search.detector_policy.min_tp_sl_margin_at_fire_candidates",
+    )
     selector_val_fires_per_30d_min = float(
         detector_policy_section.get("selector_val_fires_per_30d_min", 110.0)
     )
@@ -815,11 +837,23 @@ def _build_detector_policy_search(
             raise ValueError(
                 "search.detector_policy.peak_gain_candidates must contain values in [0, 1]"
             )
+    for value in max_sl_row_at_fire_candidates:
+        if not (0.0 <= float(value) <= 1.0):
+            raise ValueError(
+                "search.detector_policy.max_sl_row_at_fire_candidates must contain values in [0, 1]"
+            )
+    for value in min_tp_sl_margin_at_fire_candidates:
+        if not (-1.0 <= float(value) <= 1.0):
+            raise ValueError(
+                "search.detector_policy.min_tp_sl_margin_at_fire_candidates must contain values in [-1, 1]"
+            )
     return DetectorPolicySearchConfig(
         arm_candidates=arm_candidates,
         fire_candidates=fire_candidates,
         turn_candidates=turn_candidates,
         peak_gain_candidates=peak_gain_candidates,
+        max_sl_row_at_fire_candidates=max_sl_row_at_fire_candidates,
+        min_tp_sl_margin_at_fire_candidates=min_tp_sl_margin_at_fire_candidates,
         selector_val_fires_per_30d_min=selector_val_fires_per_30d_min,
         selector_val_fires_per_30d_max=selector_val_fires_per_30d_max,
         selector_min_resolved_signals=selector_min_resolved_signals,
diff --git a/pump_end_v2/detector/policy.py b/pump_end_v2/detector/policy.py
index 8e8a728..3124fbe 100644
--- a/pump_end_v2/detector/policy.py
+++ b/pump_end_v2/detector/policy.py
@@ -14,6 +14,8 @@ INPUT_REQUIRED_COLUMNS: tuple[str, ...] = (
     "decision_time",
     "entry_bar_open_time",
     "p_good",
+    "p_tp_row",
+    "p_sl_row",
     "score_source",
     "policy_context_only",
     "episode_age_bars",
@@ -57,6 +59,9 @@ CANDIDATE_LEDGER_COLUMNS: tuple[str, ...] = (
     "peak_p_good_before_fire",
     "peak_gain_from_arm_before_fire",
     "p_good_drop_from_peak",
+    "p_tp_row",
+    "p_sl_row",
+    "tp_sl_margin_at_fire",
     "distance_from_episode_high_pct",
     "episode_runup_from_open_pct",
     "episode_extension_from_open_pct",
@@ -71,6 +76,8 @@ CANDIDATE_LEDGER_COLUMNS: tuple[str, ...] = (
     "policy_fire_score_floor",
     "policy_turn_down_delta",
     "policy_min_peak_gain_after_arm",
+    "policy_max_sl_row_at_fire",
+    "policy_min_tp_sl_margin_at_fire",
     "target_good_short_now",
     "target_reason",
     "row_trade_outcome",
@@ -147,6 +154,8 @@ def build_detector_policy_runtime_cache(scored_rows_df: pd.DataFrame) -> dict[st
         )
     frame["policy_context_only"] = frame["policy_context_only"].astype(bool)
     frame["p_good"] = frame["p_good"].astype(float)
+    frame["p_tp_row"] = frame["p_tp_row"].astype(float)
+    frame["p_sl_row"] = frame["p_sl_row"].astype(float)
     if not has_fold_id:
         frame["fold_id"] = pd.NA
     sort_columns = ["score_source", "episode_id", "context_bar_open_time"]
@@ -220,12 +229,18 @@ def apply_episode_aware_detector_policy_cached(
                 continue
             peak_gain_from_arm = float(peak_p_good - p_good_at_arm)
             drop_from_peak = float(peak_p_good - current_p_good)
+            p_tp_row = float(row.p_tp_row)
+            p_sl_row = float(row.p_sl_row)
+            tp_sl_margin_at_fire = float(p_tp_row - p_sl_row)
             if (
                 peak_gain_from_arm
                 >= detector_policy_config.min_peak_gain_after_arm
                 and
                 drop_from_peak >= detector_policy_config.turn_down_delta
                 and current_p_good >= detector_policy_config.fire_score_floor
+                and p_sl_row <= detector_policy_config.max_sl_row_at_fire
+                and tp_sl_margin_at_fire
+                >= detector_policy_config.min_tp_sl_margin_at_fire
             ):
                 if bool(row.policy_context_only):
                     expired = True
@@ -264,6 +279,9 @@ def apply_episode_aware_detector_policy_cached(
                     "peak_p_good_before_fire": peak_p_good,
                     "peak_gain_from_arm_before_fire": peak_gain_from_arm,
                     "p_good_drop_from_peak": drop_from_peak,
+                    "p_tp_row": p_tp_row,
+                    "p_sl_row": p_sl_row,
+                    "tp_sl_margin_at_fire": tp_sl_margin_at_fire,
                     "distance_from_episode_high_pct": row.distance_from_episode_high_pct,
                     "episode_runup_from_open_pct": _row_value(
                         row, "episode_runup_from_open_pct"
@@ -288,6 +306,8 @@ def apply_episode_aware_detector_policy_cached(
                     "policy_fire_score_floor": detector_policy_config.fire_score_floor,
                     "policy_turn_down_delta": detector_policy_config.turn_down_delta,
                     "policy_min_peak_gain_after_arm": detector_policy_config.min_peak_gain_after_arm,
+                    "policy_max_sl_row_at_fire": detector_policy_config.max_sl_row_at_fire,
+                    "policy_min_tp_sl_margin_at_fire": detector_policy_config.min_tp_sl_margin_at_fire,
                     "bars_fire_to_ideal": bars_fire_to_ideal,
                 }
                 for column in _OPTIONAL_HINDSIGHT_COLUMNS:
diff --git a/pump_end_v2/detector/policy_search.py b/pump_end_v2/detector/policy_search.py
index 5fdc322..5a846c4 100644
--- a/pump_end_v2/detector/policy_search.py
+++ b/pump_end_v2/detector/policy_search.py
@@ -86,6 +86,8 @@ SWEEP_COLUMNS: tuple[str, ...] = (
     "fire_score_floor",
     "turn_down_delta",
     "min_peak_gain_after_arm",
+    "max_sl_row_at_fire",
+    "min_tp_sl_margin_at_fire",
     "episodes_total",
     "episodes_with_good_zone",
     "episodes_fired",
@@ -582,12 +584,31 @@ def build_detector_policy_grid(
             if len(search_config.peak_gain_candidates) > 0
             else (base_policy_config.min_peak_gain_after_arm,)
         )
+        max_sl_row_at_fire_candidates = (
+            search_config.max_sl_row_at_fire_candidates
+            if len(search_config.max_sl_row_at_fire_candidates) > 0
+            else (base_policy_config.max_sl_row_at_fire,)
+        )
+        min_tp_sl_margin_at_fire_candidates = (
+            search_config.min_tp_sl_margin_at_fire_candidates
+            if len(search_config.min_tp_sl_margin_at_fire_candidates) > 0
+            else (base_policy_config.min_tp_sl_margin_at_fire,)
+        )
         grid: list[DetectorPolicyConfig] = []
-        for arm_score_min, fire_score_floor, turn_down_delta, min_peak_gain_after_arm in product(
+        for (
+            arm_score_min,
+            fire_score_floor,
+            turn_down_delta,
+            min_peak_gain_after_arm,
+            max_sl_row_at_fire,
+            min_tp_sl_margin_at_fire,
+        ) in product(
                 search_config.arm_candidates,
                 search_config.fire_candidates,
                 search_config.turn_candidates,
                 peak_gain_candidates,
+                max_sl_row_at_fire_candidates,
+                min_tp_sl_margin_at_fire_candidates,
         ):
             if not (0.0 < float(arm_score_min) <= 1.0):
                 continue
@@ -597,21 +618,31 @@ def build_detector_policy_grid(
                 continue
             if not (0.0 <= float(min_peak_gain_after_arm) <= 1.0):
                 continue
+            if not (0.0 <= float(max_sl_row_at_fire) <= 1.0):
+                continue
+            if not (-1.0 <= float(min_tp_sl_margin_at_fire) <= 1.0):
+                continue
             grid.append(
                 DetectorPolicyConfig(
                     arm_score_min=_round6(float(arm_score_min)),
                     fire_score_floor=_round6(float(fire_score_floor)),
                     turn_down_delta=_round6(float(turn_down_delta)),
                     min_peak_gain_after_arm=_round6(float(min_peak_gain_after_arm)),
+                    max_sl_row_at_fire=_round6(float(max_sl_row_at_fire)),
+                    min_tp_sl_margin_at_fire=_round6(float(min_tp_sl_margin_at_fire)),
                 )
             )
-        unique: dict[tuple[float, float, float, float], DetectorPolicyConfig] = {}
+        unique: dict[
+            tuple[float, float, float, float, float, float], DetectorPolicyConfig
+        ] = {}
         for candidate in grid:
             key = (
                 candidate.arm_score_min,
                 candidate.fire_score_floor,
                 candidate.turn_down_delta,
                 candidate.min_peak_gain_after_arm,
+                candidate.max_sl_row_at_fire,
+                candidate.min_tp_sl_margin_at_fire,
             )
             unique[key] = candidate
         return sorted(
@@ -621,6 +652,8 @@ def build_detector_policy_grid(
                 item.fire_score_floor,
                 item.turn_down_delta,
                 item.min_peak_gain_after_arm,
+                item.max_sl_row_at_fire,
+                item.min_tp_sl_margin_at_fire,
             ),
         )
     arm_candidates = sorted(
@@ -666,6 +699,8 @@ def build_detector_policy_grid(
                     fire_score_floor=fire_score_floor,
                     turn_down_delta=turn_down_delta,
                     min_peak_gain_after_arm=base_policy_config.min_peak_gain_after_arm,
+                    max_sl_row_at_fire=base_policy_config.max_sl_row_at_fire,
+                    min_tp_sl_margin_at_fire=base_policy_config.min_tp_sl_margin_at_fire,
                 )
             )
     return grid
@@ -745,6 +780,8 @@ def sweep_detector_policy(
                     f"arm={candidate.arm_score_min:.6f} fire={candidate.fire_score_floor:.6f} "
                     f"turn={candidate.turn_down_delta:.6f} "
                     f"peak_gain={candidate.min_peak_gain_after_arm:.6f} "
+                    f"max_sl_at_fire={candidate.max_sl_row_at_fire:.6f} "
+                    f"min_tp_sl_margin={candidate.min_tp_sl_margin_at_fire:.6f} "
                     f"signals_total={len(candidate_signals_df)} "
                     f"tp_rate_resolved={float(selector_metrics['tp_rate_resolved']):.6f} "
                     f"resolved_signals_total={int(round(resolved_signals_total))} "
@@ -760,6 +797,8 @@ def sweep_detector_policy(
                 "fire_score_floor": candidate.fire_score_floor,
                 "turn_down_delta": candidate.turn_down_delta,
                 "min_peak_gain_after_arm": candidate.min_peak_gain_after_arm,
+                "max_sl_row_at_fire": candidate.max_sl_row_at_fire,
+                "min_tp_sl_margin_at_fire": candidate.min_tp_sl_margin_at_fire,
                 "episodes_total": metrics["episodes_total"],
                 "episodes_with_good_zone": metrics["episodes_with_good_zone"],
                 "episodes_fired": metrics["episodes_fired"],
@@ -884,8 +923,10 @@ def select_detector_policy(
             "fire_score_floor",
             "turn_down_delta",
             "min_peak_gain_after_arm",
+            "max_sl_row_at_fire",
+            "min_tp_sl_margin_at_fire",
         ],
-        ascending=[False, False, True, False, True, True, True, True],
+        ascending=[False, False, True, False, True, True, True, True, True, True],
         kind="mergesort",
     ).reset_index(drop=True)
     best = ranked.iloc[0]
@@ -894,6 +935,8 @@ def select_detector_policy(
         fire_score_floor=float(best["fire_score_floor"]),
         turn_down_delta=float(best["turn_down_delta"]),
         min_peak_gain_after_arm=float(best["min_peak_gain_after_arm"]),
+        max_sl_row_at_fire=float(best["max_sl_row_at_fire"]),
+        min_tp_sl_margin_at_fire=float(best["min_tp_sl_margin_at_fire"]),
     )
     log_info(
         "POLICY",
@@ -903,6 +946,8 @@ def select_detector_policy(
             f"fire={best_policy.fire_score_floor:.6f},"
             f"turn={best_policy.turn_down_delta:.6f},"
             f"peak_gain={best_policy.min_peak_gain_after_arm:.6f} "
+            f"max_sl_at_fire={best_policy.max_sl_row_at_fire:.6f} "
+            f"min_tp_sl_margin={best_policy.min_tp_sl_margin_at_fire:.6f} "
             f"tp_rate_resolved={float(best['tp_rate_resolved']):.6f} "
             f"edge_vs_breakeven_zscore={float(best['edge_vs_breakeven_zscore']):.6f} "
             f"fires_per_30d={float(best['fires_per_30d']):.6f} "
diff --git a/pump_end_v2/pipeline/run.py b/pump_end_v2/pipeline/run.py
index 67e1f27..d692baa 100644
--- a/pump_end_v2/pipeline/run.py
+++ b/pump_end_v2/pipeline/run.py
@@ -1237,6 +1237,8 @@ def _policy_to_dict(policy: Any) -> dict[str, float]:
         "fire_score_floor": float(policy.fire_score_floor),
         "turn_down_delta": float(policy.turn_down_delta),
         "min_peak_gain_after_arm": float(policy.min_peak_gain_after_arm),
+        "max_sl_row_at_fire": float(policy.max_sl_row_at_fire),
+        "min_tp_sl_margin_at_fire": float(policy.min_tp_sl_margin_at_fire),
     }
 
 
